import { Inter, Arbutus } from 'next/font/google'
 
const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})
 
const roboto_mono = Arbutus({
  weight: "400",
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-roboto-mono',
})
 